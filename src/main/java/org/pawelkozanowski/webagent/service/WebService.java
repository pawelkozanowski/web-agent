package org.pawelkozanowski.webagent.service;

import org.springframework.stereotype.Service;

//@Service
public class WebService {
//    private ResponseEntity<String> makeApiRequest(UriComponents uri) {
//        RestTemplate restClient = new RestTemplate();
//        ResponseEntity result;
//        try {
//            ResponseEntity<String> response = restClient.getForEntity(uri.toUriString(), String.class);
//            log.info(response.toString());
//            result = ResponseEntity.status(response.getStatusCode()).body(response.getBody());
//        } catch (HttpStatusCodeException e) {
//            log.info(e.toString());
//            result = ResponseEntity.status(e.getRawStatusCode()).body(e.getResponseBodyAsString());
//        }
//        //ipRouter.routeIfNeeded();
//        return result;
//    }
}
